---
import { config } from "../config";
import Nav from "astro-ui-lib/Nav";
import Container from "astro-ui-lib/Container";
import Brand from "astro-ui-lib/Brand";
import Typography from "astro-ui-lib/Typography";

---

<footer
    class="bg-gray-900 text-gray-300 py-12 mt-24"
>
    <Container
        class="flex flex-col md:flex-row justify-between items-center gap-8 text-center md:text-left">
        
        <!-- Logo y descripción -->
        <div>
            <Brand showLightLogo={false} class="h-7 w-auto mb-2"/>
            <Typography
                class="text-gray-400">
                {config.siteLegend}
            </Typography>
        </div>

        <!-- Links -->
        <div class="space-x-6">
            <Nav class="!text-gray-400" />
        </div>
    </div>

    <!-- Copyright -->
    <div class="mt-8">
        <Typography
            tag="small"
            class="text-gray-400 mb-0.5">
            © {new Date().getFullYear()} {config.site}. Todos los derechos reservados.
        </Typography>   
        <Typography
            tag="small"
            class="text-gray-400">
            Creado por <a href={config.authorUrl} target="_blank">{config.author}</a>
        </Typography> 
    </div>
    </Container>
</footer>
